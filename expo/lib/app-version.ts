/**
 * In-app display version (Settings footer).
 * - Bump the last number for each fix (patch).
 * - Bump the middle number for each push (minor).
 * - Bump the first number for major releases.
 */
export const APP_DISPLAY_VERSION = '2.4.8';
