# Audit Dev/main e release 8.5.0 — 22 settembre 2026

Stato complessivamente buono: nessun blocco della 8.5 dimostrato dai controlli eseguiti. Produzione risponde con la versione corretta e database disponibile; codice applicativo identico tra Dev, main e tag 8.5. Restano problemi operativi e di manutenzione da chiudere. Audit senza modifiche applicative, deploy, merge o mutazioni del database.

## Revisioni e pubblicazione

- `origin/Dev`: `36bfc27`; `origin/main`: `76a5bd5`; tag `v8.5.0`: `f3a39c1`.
- Release pubblicata il 21 settembre alle 15:40 UTC. Pipeline Android conclusa con successo, compresi firma, gate e generazione SBOM.
- main differisce dal tag soltanto per README. Dev differisce da main soltanto per workflow delle note di rilascio e due documenti: nessuna divergenza applicativa o SQL.
- La storia conta 6 commit esclusivi main e 19 Dev, coerentemente con promozioni squash; il confronto dei contenuti non mostra codice perso. Il branch main locale è tre commit indietro: il confronto usa i remoti aggiornati con fetch.
- Entrambi i branch richiedono i check `web`/`expo`, con protezione applicata agli amministratori e force push disabilitato.

## Rilievi prioritari

| Priorità | Rilievo ed evidenza | Impatto / prossimo intervento |
| --- | --- | --- |
| Alta operativa | Backup soltanto sulla stessa VM: `ops/supabase-backup.sh` e `docs/SUPABASE_OPERATIONS.md` lo dichiarano esplicitamente. Lo script produce dump e tar senza cifratura propria. | La perdita della VM può perdere anche i backup. Predisporre copia esterna cifrata e prova di ripristino. Configurazione effettiva della VM non riesaminata in questa sessione. |
| Media | Audit npm produzione Expo: 3 segnalazioni moderate sulla stessa catena `expo-router@57.0.21 → query-string@7.1.3 → decode-uri-component@0.2.2`. | Un'unica vulnerabilità sottostante di consumo CPU su input URI malformato; raggiungibilità sfruttabile nell'app non dimostrata. Valutare aggiornamento compatibile: il fix automatico proposto da npm retrocede Expo Router a 5.1.11 e non va applicato alla cieca. |
| Media | `/api/app-config` in produzione indica `currentVersion=8.5.0`, ma `recommendedVersion=8.2.0` e note 8.2. | La policy `lib/version-policy.ts` non segnala la 8.5 come aggiornamento disponibile a un client 8.2–8.4. Aggiornare configurazione runtime e note senza alzare necessariamente il minimo supportato, oggi 8.1. |
| Media | La release pubblica contiene soltanto l'APK; mancano `.sha256` e i due SBOM previsti dal workflow. Le note parlano di checksum allegato. | Completeness della distribuzione inferiore a quanto dichiarato. Recuperare gli artefatti della build verificata e allegarli. GitHub espone comunque il digest SHA-256 dell'APK: non è assenza totale di checksum. |
| Bassa | `app/api/admin/operations/route.ts:45` dichiara ancora ultima migrazione `20260829205816_add_win_condition_analytics.sql`, mentre il repository include `20260919120000_draw_neutral_win_rate.sql`. | Metadato diagnostico obsoleto; non prova che la migrazione manchi in produzione. Allinearlo o derivarlo automaticamente. |
| Bassa | La correzione delle note di release dinamiche è soltanto su Dev. | Una prossima release da main può ancora pubblicare il vecchio testo hardcoded. Portare la correzione su main prima del prossimo tag. Le note pubbliche della 8.5 sono già corrette. |

Advisory dipendenza: [GHSA-vcc3-ghjq-m6fr](https://github.com/advisories/GHSA-vcc3-ghjq-m6fr). Versione corretta upstream 0.5.0; l'aggiornamento richiede verifica di compatibilità nella catena Expo.

## Verifiche eseguite

| Controllo | Risultato |
| --- | --- |
| Produzione `/api/health` | HTTP 200, `ok=true` |
| Produzione `/api/ready` | HTTP 200; 8.5.0; commit `f3a39c1`; database ready, 56 ms nella singola misura |
| Quality e CodeQL remoti | Verdi sugli SHA attuali di Dev e main |
| CI web sugli SHA attuali | Migrazioni applicate e lint SQL, quality, build, budget, E2E autenticati e flusso Live completati |
| CI Expo su Dev | Quality, doctor:ci, export Android e budget completati |
| Quality Expo locale | Superata: lint, typecheck, 246 test / 67 file, loghi, knip; coverage statement 70,82% |
| Web locale | Lint superato; 255 test / 81 file superati, security e knip superati; coverage statement 57,41% |
| Typecheck web locale | Bloccato da `.next/types` obsoleti che referenziano la route avatar rimossa. È un limite dell'ambiente locale: il check pulito in CI passa sugli stessi sorgenti. Cache non rimossa durante l'audit. |
| `npm audit --omit=dev --json` | Web: 0 vulnerabilità. Expo: 3 moderate, 0 alte/critiche |
| Readiness Obtainium | Superata |
| Segreti | Nessun file `.env` reale, chiave privata o keystore tracciato; ricerca mirata dei pattern token GitHub/AWS e private key senza match su entrambi i branch. Non equivale a scansione esaustiva di tutta la storia. |

Comandi principali: `git fetch origin --prune`, `git diff origin/main origin/Dev`, `git diff v8.5.0 origin/main`, `gh release view`, `gh run list/view`, API protezioni branch, `npm run quality`, `npm --prefix expo run quality`, `npm run test:all`, `npm run knip:dependencies`, `node scripts/verify-security.mjs`, `node scripts/verify-obtainium-readiness.mjs`, audit npm e GET pubbliche sopra elencate.

## Revisione mirata e limiti

Esaminati autenticazione API con verifica server `getUser`, autorizzazione admin, cancellazione account con revoca sessioni, filtro arena pubblica, privilegi e controlli delle RPC analytics della nuova migrazione, calcolo comune delle patte, workflow release, backup e retention. Nei percorsi esaminati non emerge un bypass di autorizzazione confermato. La formula comune usa vittorie / partite decisive e gestisce il caso di sole patte.

Il backup include controlli sui percorsi temporanei e pubblicazione atomica; la documentazione riporta un restore drill del 15 settembre, non ripetuto qui. Non verificati direttamente RLS/privilegi effettivi e cron della VM, applicazione della migrazione in produzione, log Sentry, notifiche su dispositivo e firma del binario scaricato. La salute pubblica prova disponibilità puntuale, non stabilità storica o assenza di errori utente. Le build complete e gli E2E sono evidenza CI sugli SHA confrontati, non nuove esecuzioni locali.

Sono aperte 14 PR, incluse 12 Dependabot, la resilienza test Archidekt #99 e F-Droid #31. Il fallimento CodeQL osservato riguarda la PR Dependabot #65, non gli attuali Dev/main. La PR #99 modifica soltanto i test di integrazione Archidekt; non è ancora integrata. F-Droid resta fuori dal perimetro di validazione della release standard.

Evidenze remote: [Quality Dev](https://github.com/marco201091-glitch/PhyrexianArena/actions/runs/35634209694), [Quality main](https://github.com/marco201091-glitch/PhyrexianArena/actions/runs/35632398804), [build Android 8.5](https://github.com/marco201091-glitch/PhyrexianArena/actions/runs/35617058938), [release 8.5](https://github.com/marco201091-glitch/PhyrexianArena/releases/tag/v8.5.0).
